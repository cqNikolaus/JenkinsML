pipeline {
  agent any
  stages {
    stage('Build Docker Image') {
      steps {
        sh 'docker build -t train-model ./training'
      }
    }
    stage('Copy artifact') {
      steps {
        copyArtifacts(projectName: 'build-data-extraction');
      }
    }
    stage('Run Container') {
      steps {
        sh 'ls -la'
        sh 'docker run --rm -v $WORKSPACE:/workspace train-model ls -la /workspace --input_csv /workspace/build_data.csv'  // Debugging: Prüfen, ob die Datei im Container sichtbar ist
        sh 'docker run --rm -v $WORKSPACE:/workspace train-model --input_csv /workspace/build_data.csv --output_model /workspace/model.pkl'
      }
    }
  }
  post {
    always {
      archiveArtifacts artifacts: 'model.pkl', fingerprint: true
    }
  }
}
